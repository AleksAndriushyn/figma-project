import React from 'react';
import '../../styles/custom-styles/headline.scss';

const CustomHeadline = ({ children }) => {
	return <div className='headline'>{children}</div>;
};

export default CustomHeadline;
